/** @type {import("tailwindcss").Config} */
const config = {
  theme: {
    extend: {
      fontFamily: {
        serif: ["var(--font-serif)"],
        sans: ["var(--font-sans)"],
      },
    },
  },
};

export default config;
